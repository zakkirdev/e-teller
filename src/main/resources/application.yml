spring:
  datasource:
    url: jdbc:mysql://localhost:2020/eteller
    username: root
    password: root
  batch:
    initialize-schema: ALWAYS
    job:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
jwt:
  secret: secret

